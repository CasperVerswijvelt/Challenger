import { ChallengeFilterPipe } from './challenge-filter.pipe';

describe('ChallengeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChallengeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
