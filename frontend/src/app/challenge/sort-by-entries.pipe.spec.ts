import { SortByEntriesPipe } from './sort-by-entries.pipe';

describe('SortByEntriesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByEntriesPipe();
    expect(pipe).toBeTruthy();
  });
});
