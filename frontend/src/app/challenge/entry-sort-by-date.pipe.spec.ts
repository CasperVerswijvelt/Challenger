import { EntrySortByDatePipe } from './entry-sort-by-date.pipe';

describe('EntrySortByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new EntrySortByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
